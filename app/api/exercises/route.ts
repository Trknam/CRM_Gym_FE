import { NextResponse } from "next/server";
import { prisma } from "@/lib/db/prisma";
import { apiErrorFromUnknown } from "@/lib/api/response";
import { requirePermission } from "@/lib/auth/authorization";
const out = (x: any) => ({ id: x.id, name: x.name, muscle: x.muscle, equipment: x.equipment ?? "", level: x.level });
export async function GET() { try { const rows = await prisma.exercise.findMany({ where: { isActive: true }, orderBy: { name: "asc" } }); return NextResponse.json({ data: rows.map(out) }); } catch (e) { return apiErrorFromUnknown(e, "Không thể lấy kho bài tập."); } }
export async function POST(request: Request) { try { await requirePermission("member.create"); const b = await request.json(); if (!String(b.name ?? "").trim() || !String(b.muscle ?? "").trim()) return NextResponse.json({ message: "Tên bài tập và nhóm cơ là bắt buộc." }, { status: 400 }); const row = await prisma.exercise.create({ data: { name: String(b.name).trim(), muscle: String(b.muscle).trim(), equipment: String(b.equipment ?? "").trim() || null, level: String(b.level ?? "Beginner") } }); return NextResponse.json({ data: out(row) }, { status: 201 }); } catch (e) { return apiErrorFromUnknown(e, "Không thể tạo bài tập."); } }
export async function PATCH(request: Request) { try { await requirePermission("member.update"); const b = await request.json(); const row = await prisma.exercise.update({ where: { id: String(b.id) }, data: { name: String(b.name), muscle: String(b.muscle), equipment: String(b.equipment ?? "") || null, level: String(b.level ?? "Beginner") } }); return NextResponse.json({ data: out(row) }); } catch (e) { return apiErrorFromUnknown(e, "Không thể cập nhật bài tập."); } }
export async function DELETE(request: Request) { try { await requirePermission("member.delete"); const b = await request.json(); await prisma.exercise.update({ where: { id: String(b.id) }, data: { isActive: false } }); return NextResponse.json({ message: "Đã xóa bài tập." }); } catch (e) { return apiErrorFromUnknown(e, "Không thể xóa bài tập."); } }