import { Router } from "express";
import { prisma } from "../db/prisma";
import { requirePermission,getAccessibleBranchIds } from "../auth/authorization";
import { cacheDelete,cacheGet,cacheSet } from "../cache/valkey";
import { cacheKeys } from "../cache/keys";
const methods={"QR Code":"QR_CODE","Quầy lễ tân":"FRONT_DESK"} as const;const statuses={"Hợp lệ":"VALID","Từ chối":"REJECTED"} as const;const labels={QR_CODE:"QR Code",FRONT_DESK:"Quầy lễ tân",VALID:"Hợp lệ",REJECTED:"Từ chối"};
const out=(x:any)=>({id:x.id,member:x.member.fullName,memberId:x.memberId,time:x.checkedInAt.toISOString(),method:labels[x.method as keyof typeof labels],status:labels[x.status as keyof typeof labels]});
export const checkinsRoutes=Router();
checkinsRoutes.get("/",async(req,res)=>{try{await requirePermission(req,"checkin.read");const ids=await getAccessibleBranchIds(req);const rows=await prisma.checkIn.findMany({where:{branchId:{in:ids}},include:{member:true},orderBy:{checkedInAt:"desc"}});return res.json({data:rows.map(out),cached:false});}catch(error){console.error("[checkins] list failed",error);const s=(error as any)?.status;if(s)return res.status(s).json({message:(error as any).message});return res.status(500).json({message:"Không thể lấy lịch sử check-in."});}});
checkinsRoutes.post("/",async(req,res)=>{try{await requirePermission(req,"checkin.create");const b=req.body??{},memberId=String(b.memberId??"").trim();const ids=await getAccessibleBranchIds(req);const member=await prisma.member.findFirst({where:{id:memberId,branchId:{in:ids}},include:{memberships:{where:{status:"ACTIVE"},orderBy:{endDate:"desc"},take:1}}});if(!member)return res.status(404).json({message:"Không tìm thấy hội viên."});const row=await prisma.checkIn.create({data:{branchId:member.branchId,memberId,checkedInAt:b.time?new Date(b.time):new Date(),method:methods[String(b.method) as keyof typeof methods]??"FRONT_DESK",status:statuses[String(b.status) as keyof typeof statuses]??(member.memberships.length?"VALID":"REJECTED")},include:{member:true}});return res.status(201).json({data:out(row)});}catch(error){const s=(error as any)?.status;if(s)return res.status(s).json({message:(error as any).message});return res.status(500).json({message:"Không thể tạo check-in."});}});
checkinsRoutes.patch("/",async(req,res)=>{try{await requirePermission(req,"checkin.update");const b=req.body??{},id=String(b.id??"").trim(),ids=await getAccessibleBranchIds(req);const existing=await prisma.checkIn.findFirst({where:{id,branchId:{in:ids}}});if(!existing)return res.status(404).json({message:"Không tìm thấy lượt check-in."});const memberId=String(b.memberId??existing.memberId).trim();const member=await prisma.member.findFirst({where:{id:memberId,branchId:{in:ids}}});if(!member)return res.status(404).json({message:"Không tìm thấy hội viên."});const row=await prisma.checkIn.update({where:{id},data:{memberId,branchId:member.branchId,checkedInAt:b.time?new Date(b.time):existing.checkedInAt,method:methods[String(b.method) as keyof typeof methods]??existing.method,status:statuses[String(b.status) as keyof typeof statuses]??existing.status},include:{member:true}});return res.json({data:out(row)});}catch(error){console.error("[checkins] update failed",error);return res.status(500).json({message:"Không thể cập nhật check-in."});}});
checkinsRoutes.delete("/",async(req,res)=>{try{await requirePermission(req,"checkin.delete");const ids=await getAccessibleBranchIds(req);const row=await prisma.checkIn.findFirst({where:{id:String(req.body?.id),branchId:{in:ids}},select:{id:true}});if(!row)return res.status(404).json({message:"Không tìm thấy lượt check-in."});await prisma.checkIn.delete({where:{id:row.id}});return res.json({message:"Đã xóa lượt check-in."});}catch(error){console.error("[checkins] delete failed",error);return res.status(500).json({message:"Không thể xóa check-in."});}});
