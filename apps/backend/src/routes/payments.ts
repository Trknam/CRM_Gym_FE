import { Router } from "express";
import { prisma } from "../db/prisma";
import { requirePermission,getAccessibleBranchIds } from "../auth/authorization";
import { cacheDelete,cacheGet,cacheSet } from "../cache/valkey";
import { cacheKeys } from "../cache/keys";
const methodMap:Record<string,"CASH"|"BANK_TRANSFER"|"CARD">={"Tiền mặt":"CASH","Chuyển khoản":"BANK_TRANSFER","Thẻ":"CARD"};
const statusMap:Record<string,"PAID"|"PENDING"|"CANCELLED">={"Đã thanh toán":"PAID","Chờ thanh toán":"PENDING","Đã hủy":"CANCELLED"};
const methods={CASH:"Tiền mặt",BANK_TRANSFER:"Chuyển khoản",CARD:"Thẻ"};const statuses={PAID:"Đã thanh toán",PENDING:"Chờ thanh toán",CANCELLED:"Đã hủy"};
const out=(x:any)=>({id:x.id,member:x.member.fullName,memberId:x.memberId,amount:Number(x.amount),method:methods[x.method as keyof typeof methods],status:statuses[x.status as keyof typeof statuses],date:(x.paidAt??x.createdAt).toISOString().slice(0,10)});
export const paymentsRoutes=Router();
paymentsRoutes.get("/",async(req,res)=>{try{await requirePermission(req,"payment.read");const ids=await getAccessibleBranchIds(req);const rows=await prisma.payment.findMany({where:{branchId:{in:ids}},include:{member:true},orderBy:{createdAt:"desc"}});return res.json({data:rows.map(out),cached:false});}catch(error){const s=(error as any)?.status;if(s)return res.status(s).json({message:(error as any).message});return res.status(500).json({message:"Không thể lấy giao dịch."});}});
paymentsRoutes.post("/",async(req,res)=>{try{await requirePermission(req,"payment.create");const b=req.body??{},memberId=String(b.memberId??"").trim(),amount=Number(b.amount);const ids=await getAccessibleBranchIds(req);const member=await prisma.member.findFirst({where:{id:memberId,...(ids===null?{}:{branchId:{in:ids}})}});if(!member||!Number.isFinite(amount)||amount<0)return res.status(400).json({message:"Hội viên hoặc số tiền không hợp lệ."});const row=await prisma.payment.create({data:{branchId:member.branchId,memberId,amount,method:methodMap[String(b.method)]??"CASH",status:statusMap[String(b.status)]??"PAID",paidAt:b.date?new Date(b.date):new Date()},include:{member:true}});await cacheDelete(cacheKeys.payments("all"));return res.status(201).json({data:out(row)});}catch{return res.status(500).json({message:"Không thể tạo giao dịch."});}});
paymentsRoutes.patch("/",async(req,res)=>{try{await requirePermission(req,"payment.update");const b=req.body??{},ids=await getAccessibleBranchIds(req);const existing=await prisma.payment.findFirst({where:{id:String(b.id),...(ids===null?{}:{branchId:{in:ids}})}});if(!existing)return res.status(404).json({message:"Không tìm thấy giao dịch."});const row=await prisma.payment.update({where:{id:existing.id},data:{amount:Number(b.amount??existing.amount),method:methodMap[String(b.method)]??existing.method,status:statusMap[String(b.status)]??existing.status,paidAt:b.date?new Date(b.date):existing.paidAt},include:{member:true}});await cacheDelete(cacheKeys.payments("all"));return res.json({data:out(row)});}catch{return res.status(500).json({message:"Không thể cập nhật giao dịch."});}});
paymentsRoutes.delete("/",async(req,res)=>{try{await requirePermission(req,"payment.delete");const ids=await getAccessibleBranchIds(req);const existing=await prisma.payment.findFirst({where:{id:String(req.body?.id),...(ids===null?{}:{branchId:{in:ids}})}});if(!existing)return res.status(404).json({message:"Không tìm thấy giao dịch."});await prisma.payment.update({where:{id:existing.id},data:{status:"CANCELLED"}});await cacheDelete(cacheKeys.payments("all"));return res.json({message:"Đã hủy giao dịch."});}catch{return res.status(500).json({message:"Không thể hủy giao dịch."});}});
