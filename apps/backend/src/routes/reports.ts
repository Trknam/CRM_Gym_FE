import { Router } from "express";
import { requirePermission,getAccessibleBranchIds } from "../auth/authorization";
import { getDashboardMetrics } from "../services/dashboard-metrics";
import { cacheGet,cacheSet,cacheDelete } from "../cache/valkey";
import { cacheKeys } from "../cache/keys";
export const reportsRoutes=Router();
reportsRoutes.get("/",async(req,res)=>{try{await requirePermission(req,"report.read");const ids=await getAccessibleBranchIds(req);const key=cacheKeys.reports(ids===null?"all":ids.sort().join(",")||"none");const cached=await cacheGet<any[]>(key);if(cached)return res.json({data:cached,cached:true});const m=await getDashboardMetrics(ids);const data=[{id:"revenue-month",name:"Doanh thu tháng",type:"Doanh thu",period:"Tháng hiện tại",status:`${m.finance.monthRevenue.toLocaleString("vi-VN")} VNĐ`},{id:"revenue-total",name:"Tổng doanh thu",type:"Doanh thu",period:"Tất cả thời gian",status:`${m.finance.allTimeRevenue.toLocaleString("vi-VN")} VNĐ`},{id:"collection",name:"Đã thu",type:"Thanh toán",period:"Khoản đã thanh toán",status:`${m.finance.allTimeRevenue.toLocaleString("vi-VN")} VNĐ`},{id:"due",name:"Còn phải thu",type:"Thanh toán",period:"Giao dịch chờ thanh toán",status:`${m.finance.pendingAmount.toLocaleString("vi-VN")} VNĐ`},{id:"members",name:"Hội viên",type:"Hội viên",period:"Hiện tại",status:`${m.members.total} hội viên / ${m.members.active} đang hoạt động`},{id:"checkins",name:"Check-in",type:"Check-in",period:"7 ngày gần nhất",status:`${m.attendance.last7Days} lượt hợp lệ`},{id:"leads",name:"CRM",type:"Leads",period:"Cần xử lý",status:`${m.crm.dueFollowUps} follow-up / ${m.crm.inactive14Days} hội viên ít hoạt động`}];await cacheSet(key,data,30);return res.json({data,cached:false});}catch(error){const s=(error as any)?.status;if(s)return res.status(s).json({message:(error as any).message});console.error("[reports] failed",error);return res.status(500).json({message:"Không thể tạo báo cáo từ dữ liệu PostgreSQL."});}});
